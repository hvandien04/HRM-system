spring:
  application:
    name: hrm-system
  datasource:
    url: ${POSTGRES_URL}
    username: ${POSTGRES_USERNAME}
    password: ${POSTGRES_PASSWORD}
  data:
    redis:
      host: ${REDIS_ADDR:redis}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      username: ${REDIS_USER:}
#  mail:
#    host: ${MAIL_HOST}
#    port: ${MAIL_PORT}
#    username: apikey
#    password: ${SEND_GRID_API_KEY}
#    from-email: ${MAIL_FROM_NAME}
#    properties:
#      mail:
#        smtp:
#          auth: true
#          starttls:
#            enable: true

springdoc:
  swagger-ui:
    disable-swagger-default-url: true
    url: ../v3/api-docs
    config-url: ../v3/api-docs/swagger-config
